# متتبّع المراحل

[← العودة للفهرس](../README.md)

> **اصطلاح الحالة:** `✅` مكتملة · `⏳` جارية · `⬜` لم تبدأ · `🚫` مُلغاة
>
> **المراحل غير المنتهية تبقى مدرجة بتفصيلها الكامل حتى تُنجز فعليًا.** لا تُحذف ولا تُختصر.
> المرحلة تُعلَّم `✅` فقط بعد اجتياز بوابة الخروج الخاصة بها، ويُسجَّل تاريخها.

---

# نظرة عامة

## المسار حتى الإطلاق

| # | المرحلة | المدة | الحالة | تاريخ الإنجاز |
|---|---|---|---|---|
| **0** | الاكتشاف والتدقيق المعماري | — | ✅ **مكتملة** | 2026-08-12 |
| **0.1** | إصلاح أمني عاجل | ٣ أيام | ✅ **مكتملة** | 2026-08-12 |
| **0.5** | إعادة الهيكلة النطاقية | ٧-١٠ أيام | ✅ **مكتملة** | 2026-08-12 |
| **1** | الأساس: User + Decimal + i18n + البريد | ٢ أسبوع | ✅ **مكتملة** | 2026-08-12 |
| **1.5** | بذرة الفرونت إند + نظام الثيم | ٢-٣ أسابيع | ⏳ **جارية** — الأساس والثيم والمصادقة جاهزة | — |
| **2** | RBAC + سياسات الوصول + المراقبة | ٢ أسبوع | ✅ **مكتملة** | 2026-08-12 |
| **3** | الكتالوج | ٣ أسابيع | ⏳ **الباك إند مكتمل** | — |
| **4** | المخزون والدفعات | ٢ أسبوع | ⏳ **الباك إند مكتمل** | — |
| **5** | التسعير + السلة + الطلب + الدفع | ٣ أسابيع | ⏳ **الباك إند مكتمل** | — |
| **6** | متجر الطلاب — 🚀 **الإطلاق** | ٣ أسابيع | ⏳ **الباك إند مكتمل جزئيًا** | — |

> ⚠️  **`⏳` ليست `✅`.**
>
> نطاقات المراحل ٣–٦ مبنية ومُختبَرة في الباك إند، لكن **لا شاشة
> واحدة تستهلكها**. المرحلة لا تُغلَق ببناء نصفها: بوابة الخروج
> للمرحلة ٦ هي الإطلاق، ولا إطلاق بلا واجهة.
>
> ما بقي في كل مرحلة مذكور تحتها بالتفصيل — ولا يُحذف حتى يُنجَز.

**الفريق: مطوّر واحد.** التقدير حتى الإطلاق: **٢١-٢٥ أسبوعًا** (~٥-٦ أشهر).

> **توصية على الترتيب لمطوّر واحد:** أنجز الباك إند لكل مرحلة كاملًا ثم واجهتها، لا بالتوازي. تبديل السياق بين Django و React عدة مرات يوميًا يكلّف أكثر مما يوفّر حين يكون المنفّذ شخصًا واحدًا.

## ما بعد الإطلاق

| # | المرحلة | المدة | الحالة |
|---|---|---|---|
| 7 | **نظام نقطة البيع (POS)** | ٣-٤ أسابيع | ✅ **مكتملة 2026-08-15** — قُدّمت لأن فرعًا فعليًا يعمل (قاعدة ١٠) |
| 8 | **المالية — الإيرادات والمصروفات** | ٢-٣ أسابيع | ✅ **مكتملة 2026-08-15** |
| 9 | **B2B الصيدليات** | ٣ أسابيع | ✅ **مكتملة 2026-08-15** |
| 10 | بوابة الموظفين | ٣ أسابيع | ⬜ |
| 11 | التارجت الشهري والعمولات | ٣ أسابيع | ⬜ |
| 12 | الولاء والإحالة وسفراء الجامعات | ٢ أسبوع | ⬜ |
| 13 | التقارير والتحليلات | ٢ أسبوع | ⬜ |
| 14 | الموردون | ٢ أسبوع | ⬜ |
| 15 | Marketplace | — | ⬜ |
| — | الأدوية الموصوفة + سير الوصفات | — | 🚫 مؤجلة (OTC فقط) |

> **✅ قُدِّم POS فعلًا.** قاعدة العمل ١٠ حُسمت بـ«نعم — فرع يعمل بالفعل» (2026-08-14)، فنُفِّذت المرحلة ٧ قبل بقية ما بعد الإطلاق.
>
> **لكن هذا لا يؤثر على التصميم:** حقول POS (`Order.channel` · `Order.location` · `StockLocation`) تُخلق في المرحلتين ٤ و٥ **مهما كان ترتيبه** — انظر [الحقول المبكرة الإلزامية](../backend/05-MIGRATION.md).

---

# ✅ المرحلة 0 — الاكتشاف والتدقيق (مكتملة 2026-08-12)

- [x] تدقيق كامل للمستودع بقراءة الكود
- [x] كشف طبيعة `full-temp/` الحقيقية (قالب HTML لا تطبيق)
- [x] توثيق ٨ انتهاكات حدود + تبعية دائرية مؤكدة
- [x] توثيق ١٦ خطأً حرجًا بمواقعها
- [x] خريطة النطاقات + مخطط التبعيات + عقود الفرض
- [x] استراتيجية الانتقال + بوابات الجودة
- [x] فصل الوثائق إلى `shared/` + `backend/` + `frontend/`

**المخرج:** [مجلد `docs/`](../README.md) الكامل.

---

# ✅ المرحلة 0.1 — إصلاح أمني عاجل (مكتملة 2026-08-12)

- [x] نقل كل الأسرار إلى `.env` عبر `django-environ`
- [x] توليد `SECRET_KEY` جديد
- [x] `DEBUG=False` افتراضيًا + `ALLOWED_HOSTS` من البيئة
- [x] إزالة `db.sqlite3` من تتبع Git
- [x] `full-temp/` في `.gitignore`
- [x] فصل الإعدادات: `base.py` / `dev.py` / `prod.py`
- [x] `.gitignore` شامل + `.env.example`
- [x] `debug_toolbar` في التطوير فقط
- [x] تشديد HTTPS و HSTS والكوكيز في الإنتاج
- [x] بريد مثبّت في `accounts/views.py` ⟵ `settings.DEFAULT_FROM_EMAIL`
- [x] تنظيف `requirements.txt` (حذف PyQt5 و`django-context-cache` و~٣٠ تبعية غير مباشرة)
- [x] بيئة معزولة `.venv` — البيئة العامة كانت على Django 5.2 والمشروع على 4.2
- [ ] ⚠️ **إبطال كلمة مرور SMTP المسرّبة من حساب Google** ← **بيد المالك — لم يُنفَّذ بعد**

**بوابة الخروج:** ✅ صفر أسرار في الملفات المتتبَّعة · `check` و`check --deploy` بلا مشاكل · Git خالٍ من قاعدة البيانات والقالب.

> ⚠️ **بند واحد معلّق:** كلمة المرور أُزيلت من الكود لكنها **ما زالت صالحة** في حساب Google وموجودة في تاريخ Git. إبطالها إجراء يدوي لا يمكن أتمتته.

---

# ✅ المرحلة 0.5 — إعادة الهيكلة النطاقية (مكتملة 2026-08-12)

## الهيكل

- [x] `project/` ← `config/`
- [x] `core/` — نماذج أساسية · UUIDv7 · المال · التدقيق · الأخطاء · الترقيم · الضريبة · الإعدادات
- [x] ١٣ نطاق سقالة بحدود معرّفة + `README.md` لكل واحد
- [x] حذف كل `templates/` والـ views المرندرة و context processors *(قرار SPA)*
- [x] حذف تطبيقَي `products/` و `settings/` القديمين
- [x] حذف نماذج `orders/` القديمة *(FloatField · IDOR · APIs لا تعمل)*
- [x] إعادة تهيئة قاعدة البيانات مرة واحدة

## الهوية

- [x] `accounts.User` مخصص بـ **UUIDv7** — نحيف، صفر حقول تجارية
- [x] `AccountStatus` منفصل عن `VerificationStatus`
- [x] `UserSession` · `AccountStatusChange` · `SecurityToken`
- [x] باكند مصادقة يفحص الحالة *(القديم كان يُدخِل الموقوفين)*

## المعرّفات والمال

- [x] `core.identifiers` — `secrets` بدل `random` · أرقام عمل Crockford · أسماء ملفات عشوائية
- [x] `core.money` — `Decimal` حصرًا · `ROUND_HALF_UP` · تسلسل نصي
- [x] `core.models.tax` — `TaxClass` بفترة صلاحية

## الأدوات

- [x] **`import-linter` — ٥ عقود · صفر انتهاك**
- [x] `ruff` + `pytest` + `pre-commit` + CI على GitHub Actions
- [x] **٣٤ اختبارًا** تحرس القرارات المعمارية

**بوابة الخروج:** ✅ `check` · `check --deploy` · `makemigrations --check` · `lint-imports` · `pytest` · `ruff` — كلها خضراء.

## مؤجّل من هذه المرحلة

| البند | السبب | متى |
|---|---|---|
| **PostgreSQL** | غير مثبّت على الجهاز ولا Docker | **إلزامي قبل المرحلة ٤** — `select_for_update` لا يعمل على SQLite فمنع البيع الزائد مستحيل |
| ترقية Django 4.2 → 5.2 | البيئة العامة على 5.2 والمشروع على 4.2 | المرحلة ١ |
| `reporting/` | لا تقارير بعد | المرحلة ١٣ |

---

# ✅ المرحلة 1 — الأساس (مكتملة 2026-08-12)

> **المنجَز:** ترقية Django 5.2 · نطاقات الهوية الثلاثة ·
> دورة المصادقة كاملة · البريد ثنائي اللغة · الإيقاف الفوري ·
> واجهات العملاء بفحص ملكية · بوابة الأدمن للحسابات والمراقبة ·
> **١٠٤ اختبارات**.
>
> **المتبقي:** تصريف الترجمات (محجوب بـ gettext) · تغيير البريد
> بتأكيد مزدوج · روابط ملفات موقّعة بصلاحية زمنية.

## ✅ منجَز

- [x] `AUTH_USER_MODEL` مخصص + `AccountType` + `VerificationStatus` + `AccountStatus`
- [x] **إبطال فوري للجلسات** — كاش + قائمة سوداء + Access بعمر ١٠ دقائق
- [x] `customers/` — ملف · وثائق · عناوين · واجهات بفحص ملكية
- [x] `administration/` — ملف · أدوار · إسناد زمني
- [x] **بوابة الأدمن**: إيقاف/تفعيل · من متصل الآن · آخر ظهور · **آخر عملية** · مدة الاستخدام
- [x] `UUIDPrimaryKeyModel` + `BigInt` للجداول الداخلية + `slug` (المرحلة ٣)
- [x] `core.identifiers` بـ `secrets` + أرقام عمل Crockford + أسماء ملفات عشوائية
- [x] `DecimalField` + `core.money` · نماذج أساسية · `TaxClass`
- [x] أكواد أخطاء ثابتة مترجمة + معالج موحّد
- [x] تفاوض اللغة (ترويسة ← تفضيل المستخدم ← افتراضي ← عربية)
- [x] `django-cors-headers` + تدوير Refresh + Blacklist
- [x] `core.mail` + تفعيل الحساب + استرجاع كلمة المرور
- [x] تحديد معدل مشدّد على نقاط المصادقة
- [x] `flush_presence` — تفريغ التواجد المجمّع
- [x] ترقية Django 4.2 → 5.2

## ✅ منجَز — الدفعة الثانية

- [x] **تغيير البريد بتأكيد من العنوانين** — تحذير للقديم + رمز للجديد
- [x] **روابط ملفات موقّعة** بصلاحية ٥ دقائق تحمل معرّف المستخدم
- [x] فحص نوع وحجم المرفوعات بقائمة بيضاء
- [x] `conftest` — وسائط معزولة وكاش نظيف بين الاختبارات

## ⏸️ مؤجّل بقرار — إلى نهاية الخطة

- [ ] **تصريف ملفات الترجمة (`.mo`)**

> **القرار:** الترجمة تُنجَز دفعة واحدة في نهاية الخطة، لا تدريجيًا.
>
> **السبب:** النصوص المعروضة تتغير مع كل مرحلة. ترجمتها الآن تعني
> إعادة ترجمة ما يتغير لاحقًا. والأثر الحالي محدود: رسائل النظام
> تظهر بالعربية للجميع — مقبول أثناء التطوير، غير مقبول عند الإطلاق.
>
> **الأداة:** `Babel` مثبّت (`pybabel`) ويغني عن GNU gettext على ويندوز
> — ينتج ملفات `.po` و`.mo` قياسية يقرأها Django بلا فرق.
>
> **موعد الإنجاز الإلزامي:** قبل المرحلة ٦ (الإطلاق).

**بوابة الخروج:** `AUTH_USER_MODEL` مستقر · صفر `FloatField` للمال · صفر معرّف تسلسلي · دورة مُختبَرة كاملة ✅

---

# 📦 أرشيف — تفصيل المرحلة ١ الأصلي

## الهوية

- [ ] `AUTH_USER_MODEL` مخصص + `AccountType` + `VerificationStatus`
- [ ] **`AccountStatus`** (Active · Suspended · Blocked) + سبب + مَن + متى ← منفصل عن `VerificationStatus`
- [ ] **إبطال فوري للجلسات عند الإيقاف** — قائمة سوداء + مجموعة Redis + Access Token بعمر ١٠ دقائق
- [ ] `customers/` — ملف العميل الخارجي
- [ ] `administration/` — مديرو النظام

## المعرّفات — 🔒 يُحسم الآن أو لا يُحسم أبدًا

> تغيير نوع المفتاح الأساسي بعد تراكم البيانات **أشد ألمًا من تغيير `AUTH_USER_MODEL`** — يمس كل مفتاح أجنبي في القاعدة. التفاصيل في [backend/06-IDENTIFIERS.md](../backend/06-IDENTIFIERS.md).

- [ ] `UUIDPrimaryKeyModel` (**UUIDv7**) في `core/models/` — لكل نموذج يظهر في رابط أو استجابة
- [ ] الجداول الداخلية عالية الحجم تبقى `BigInt` (`AuditLog` · `StockMovement` · `UserSession` · `PaymentTransaction`)
- [ ] `slug` فريد **وثابت** للكتالوج + تحويل ٣٠١ عند التغيير
- [ ] إصلاح `products/models.py:30-32` — الـ slug يُعاد توليده في كل حفظ حاليًا
- [ ] `BusinessNumberField` + مولّد Crockford Base32 (`ORD-2026-7K3M9P`)
- [ ] **استبدال `utils/generate_code.py`** — يستخدم `random` غير الآمن تشفيريًا لكود **تفعيل الحساب**
- [ ] تخزين الملفات بأسماء عشوائية ومسارات مجزّأة (حاليًا `media/brand/01.jpg` قابلة للتعداد)
- [ ] روابط موقّعة بصلاحية زمنية للملفات الحساسة
- [ ] توحيد `404` لغير الموجود وغير المملوك — الفرق بين `403` و`404` أداة تعداد
- [ ] ترقيم بالمؤشر (cursor) لا بالإزاحة على القوائم الحساسة

## البنية التحتية

- [ ] `DecimalField` لكل مبلغ + نوع `Money` في `core`
- [ ] نماذج أساسية: `TimeStamped` · `SoftDelete` · `Audited` · `Translatable`
- [ ] بنية ترجمة المحتوى (عربي/إنجليزي)
- [ ] أكواد أخطاء ثابتة قابلة للترجمة
- [ ] تفاوض اللغة عبر `Accept-Language`
- [ ] `django-cors-headers` + تدوير Refresh Token + Blacklist
- [ ] تصريف ملفات الترجمة (`.mo`) — غائبة حاليًا

## البريد

- [ ] `core/mail` + SMTP من البيئة + قوالب ثنائية اللغة
- [ ] **تفعيل الحساب بالبريد** — إعادة بناء النظام المعطوب (`accounts/views.py:27-29`)
- [ ] **استرجاع كلمة المرور بالبريد** — رمز موقّع · صلاحية زمنية · استخدام واحد · تحديد معدل
- [ ] تغيير البريد بتأكيد من العنوانين

**بوابة الخروج:** `AUTH_USER_MODEL` مستقر نهائيًا · صفر `FloatField` للمال · **صفر معرّف تسلسلي في أي رابط أو استجابة** · دورة مُختبَرة كاملة: تسجيل ← تفعيل ← دخول ← نسيان ← استرجاع ← دخول.

---

# ⏳ المرحلة 1.5 — بذرة الفرونت إند ونظام الثيم (٢-٣ أسابيع)

## ✅ منجَز — بذرة بيانات التطوير (2026-08-13)

- [x] `devtools/` — تطبيق مستقل فوق كل النطاقات، **غير مثبّت في الإنتاج**
- [x] `seed_dev` — نظام كامل قابل للتجربة في أمر واحد، قابل للتشغيل مرارًا
- [x] ٢٣ منتجًا · ١٠ نسخ · ١١ فئة · ٣ مواقع مخزنية · ٤ مناطق شحن
- [x] ١٧ حسابًا يغطي كل نوع وحالة (موقوف · ينتظر التوثيق · مرفوض)
- [x] ٦ طلبات عبر **المسار الحقيقي** — بحجز مخزون ولقطات أسعار وسجل حالة
- [x] حالات حدّية مقصودة: دفعة منتهية · دفعة توشك · نسخة نافدة · كوبون منتهٍ
- [x] `--minimal` (بنية فقط) · `--reset` (حذف وإعادة بناء)

> ⚠️  **الفرونت إند كان محجوبًا بلا هذا.** قاعدة فارغة تعني شاشات
> تُبنى على بيانات متخيَّلة، فتنكسر عند أول اتصال حقيقي.

## ✅ منجَز — قاعدة الفرونت إند (2026-08-14)

- [x] **React 19 + Vite 6 + TypeScript صارم** (ADR-37 ينقض ADR-03)
- [x] `shared/http` — المصدر الوحيد لـ `base_url` + قاعدة ESLint تفرضه
- [x] RTL/LTR بخصائص منطقية — **تبديل لغة فوري بلا إعادة جلب**
- [x] `branding/` في الباك إند — لوحتان · تباين WCAG AA مفروض · معاينة لا تكتب
- [x] حقن رموز الثيم وقت التشغيل — صفر لون مكتوب يدويًا
- [x] هيدر وفوتر ولوجو **منفصل لكل بوابة**
- [x] `ListLayout` · `SidebarLayout` · `PageHeader` · `Drawer`
- [x] `Button` · `Badge` · `Field` · `Alert` · `Spinner` · `Skeleton` · `StateMessage`
- [x] **المصادقة**: دخول · تدوير توكن · استعادة جلسة · حارس مسار · قائمة حساب
- [x] شاشة المنتجات: بحث مؤجَّل · فلترة من الخادم · ترقيم بالمؤشر
- [x] كل بوابة حزمة منفصلة (`lazy`)

## ✅ منجَز — المتجر كاملًا (2026-08-14)

- [x] **تفاصيل المنتج**: معرض · نسخ · مواصفات دوائية · توفر · تقييمات
- [x] **التسجيل** · تفعيل البريد · استرجاع كلمة المرور — على مسارات
      `/auth/*` المطابقة لما يرسله الخادم حرفيًا
- [x] **بوابة الحساب**: البيانات · العناوين · الوثائق · الأمان
- [x] **حزم الطلاب**: القائمة والتفاصيل والإضافة الكاملة أو الأساسية
- [x] إلغاء الطلب بسبب مسجَّل
- [x] رفع الوثائق برابط موقّع — المسار المباشر لا يُرجَع أبدًا

**بوابة الخروج للمتجر:** ✅ كل مسار عميل مبنيّ ومُختبَر مقابل الخادم.

## ✅ منجَز — لوحة الأدمن والبريد والمهام (2026-08-14)

- [x] **`DataTable`** يتحوّل إلى بطاقات على الهاتف · `FilterBar` ·
      `StatusTabs` · `Modal` · `Toast` · `StatCard` · `Pagination`
- [x] **لوحة معلومات** بأرقام تستدعي تصرّفًا لا أرقام للعرض
- [x] **الطلبات**: قائمة مفلترة · تفاصيل · انتقالات الحالة من آلة الحالة
- [x] **المنتجات** · **المخزون** (تنبيهات + أرصدة + صيانة يدوية)
- [x] **الحسابات**: إيقاف/تفعيل بأثر فوري · من متصل الآن (تحديث دوري)
- [x] **الضريبة**: نسبة · إيقاف شامل · فئات معفاة · عدد المنتجات المتأثرة
- [x] **بوابات الدفع**: تشغيل وإيقاف بأثر فوري + سبب مُدقَّق
- [x] **تحرير الهوية البصرية**: معاينة معزولة + فحص تباين يمنع الحفظ
- [x] **قوالب بريد الطلبات** ثنائية اللغة: استلام · تأكيد · شحن ·
      تسليم · إلغاء · استلام دفع
- [x] **`ops/`** — طابور المهام الدورية، مثبّت في الإنتاج

**بوابة الخروج:** ✅ الأدمن يدير كل شيء من اللوحة بلا `manage.py`.

## ✅ منجَز — البنية الأمامية والهوية (2026-08-14 · 2026-08-15)

- [x] عميل HTTP موحّد بـ **`base_url` من متغير بيئة** + قاعدة ESLint تمنع العناوين اليدوية
- [x] RTL/LTR + `react-i18next` — تبديل لغة **فوري بلا شبكة** (ADR-34)
- [x] قشرات البوابات: المتجر · الحساب · الأدمن · نقطة البيع
- [x] تخطيطات: `SidebarLayout` · `ListLayout` · `PageHeader`
- [x] مكوّنات مشتركة: `StatCard` · `DataTable` · `FilterBar` · `StatusTabs` ·
      `StateMessage` · `Pagination` · `Drawer` · `Modal` · `Toast` · `Skeleton`
- [x] **رفع صور المنتجات** — رفع · ترتيب · صورة رئيسية · حذف
- [x] `branding/` كاملًا — ألوان · لوجو · لوحتان (فاتح/داكن) · كاش ١٢ ساعة
- [x] حقن الثيم من الجذر لمنع وميض الألوان
- [x] شاشة الأدمن للهوية + معاينة حية + **فحص تباين WCAG AA يمنع الحفظ** (ADR-40)

## ⬜ متبقٍّ

- [ ] تخطيطات لم تُحتَج بعد: `TabbedLayout` · `DetailLayout` · `FormLayout` ·
      `SettingsLayout` · `DashboardLayout` · `FeatureLayout`
      — **تُبنى عند أول شاشة تطلبها لا قبلها**
- [ ] قشرة الموظفين (تتبع المرحلة ١٠)
- [ ] الإشعارات داخل التطبيق بالعربية فقط — البريد ثنائي اللغة والإشعار لا

**بوابة الخروج:** تغيير اللون الأساسي من الأدمن ينعكس على كل البوابات بلا إعادة نشر · صفر عنوان API مكتوب يدويًا.

---

# ✅ المرحلة 2 — RBAC وسياسات الوصول والمراقبة (مكتملة 2026-08-12)

- [x] `core/permissions` — أوليات الصلاحيات (`IsAdminAccount` لا `is_staff`)
- [x] `administration.AdminRole` + إسناد زمني للتدقيق
- [x] **`access/`** — محرك سياسات المنتجات المركزي
- [x] `PolicyAwareQuerySetMixin` — الفلترة في الـ queryset لا الواجهة
- [x] **٣٣ خانة في مصفوفة الأمان**: سياسة × نوع حساب × توثيق
- [x] **وضع معاينة الأدمن** بقيود: أدمن · قراءة · بلا صلاحيات · مُسجَّل
- [x] مصفوفة «من يرى ماذا» كأداة تشخيص للأدمن
- [x] `UserSession` — دخول · خروج · آخر نشاط · IP · جهاز · مدة
- [x] التواجد الآني + `flush_presence`
- [x] شاشة إدارة الحسابات: إيقاف/تفعيل · آخر عملية · مدة الاستخدام
- [x] `core/audit` + سجل التدقيق العام

**بوابة الخروج:** ✅ مصفوفة أمان كاملة · إيقاف حساب يقطع الوصول فورًا (مُختبَر بنفس التوكن)

---

# ⏳ المرحلة 3 — الكتالوج (الباك إند مكتمل)

- [x] `Product` + `Variant` + فئات شجرية + `Brand` + `Manufacturer`
- [x] حقول OTC: مادة فعالة · تركيز · شكل دوائي · حجم عبوة · شروط تخزين · رقم تسجيل
- [x] **`requires_prescription` + `regulatory_class`** ← تُضاف الآن، تُستخدم لاحقًا
- [x] محتوى ثنائي اللغة
- [x] بحث وفلترة واعيان بالسياسات
- [x] `reviews/` مع verified purchase + moderation

## ⬜ متبقٍّ

- [ ] شاشات الكتالوج للعميل (قائمة · تفاصيل · بحث · فلاتر)
- [ ] شاشات إدارة الكتالوج للأدمن
- [x] **رفع صور المنتجات** — بفحص **توقيع الملف** لا ترويسته (ADR-45) · حد ٨ صور · صورة رئيسية واحدة يفرضها قيد قاعدة بيانات · حذف الرئيسية يرقّي التالية

---

# ⏳ المرحلة 4 — المخزون (الباك إند مكتمل)

- [x] **`StockLocation`** (مخزن / فرع) ← **إلزامي الآن حتى لو كان موقعًا واحدًا**
- [x] `Stock` لكل (منتج × موقع): Physical / Reserved / Available / Damaged / Expired
- [x] `StockMovement` بكل الأنواع + **`Transfer`** بين المواقع
- [x] `Batch` + تاريخ الصلاحية + **تكلفة الشراء** (لـ COGS في المرحلة ٨) + **FEFO**
- [x] الحجز والإفراج
- [x] منع البيع الزائد عبر `select_for_update` + `transaction.atomic` + قيد قاعدة بيانات
- [x] تنبيهات النفاد وقرب الصلاحية مع منع التكرار
- [x] الجرد (Stock Count) والتسويات

**بوابة الخروج:** ✅ اختبار تزامن يثبت استحالة البيع الزائد — يعمل على PostgreSQL بلا تخطٍّ.

## ⬜ متبقٍّ

- [ ] شاشات المخزون: الأرصدة · الحركات · الدفعات · الجرد · التحويلات
- [ ] مهام دورية: إفراج الحجوزات المنتهية · حجر الدفعات المنتهية · فحص التنبيهات

---

# ⏳ المرحلة 5 — التسعير والسلة والطلب والدفع (الباك إند مكتمل)

- [x] `pricing/` — محرك مركزي (سعر حسب نوع الحساب + كميات + عروض)
- [x] `promotions/` — كوبونات موحّدة بتنفيذ واحد
- [x] `cart/` — إعادة تحقق كاملة عند كل عملية
- [x] `orders/` — state machine صريحة بانتقالات مسموحة فقط
- [x] **`Order.channel`** (ONLINE / POS / EMPLOYEE / PHONE) ← إلزامي الآن
- [x] **`Order.location`** → FK إلى `StockLocation` ← إلزامي الآن
- [x] **حقول الإسناد** (`created_by` · `owner_employee` · `commission_employee`) ← تُخلق وتبقى فارغة
- [x] لقطة أسعار على مستوى سطر الطلب
- [x] `payments/` — **سجل بوابات قابل للضبط من الأدمن** (لا بوابة مثبتة في الكود)
- [x] **تشغيل وإيقاف البوابات من اللوحة** بأثر فوري — قاعدة العمل ٦ (2026-08-13)
- [x] `shipping/` — مناطق ورسوم وشحنات

**بوابة الخروج:** ✅ كل انتقال حالة غير مسموح مرفوض ومُختبَر · إضافة بوابة دفع لا تحتاج تعديل كود `orders`.

## ⬜ متبقٍّ

- [ ] محوّلات البوابات الخارجية (Paymob · Fawry) — تنتظر اختيار المزوّد
- [ ] شاشات: السلة · إتمام الشراء · تتبّع الطلب · إدارة الطلبات للأدمن

---

# ⏳ المرحلة 6 — متجر الطلاب — 🚀 الإطلاق (الباك إند جزئي)

- [x] `academic/` — جامعة / كلية / قسم / سنة دراسية
- [x] الحزم الدراسية والمستلزمات المطلوبة والموصى بها
- [x] أسعار وخصومات الطلاب — **قائمة أسعار منفصلة** لا نسبة (قاعدة ٩)
- [x] كتالوجات خاصة بكل جامعة (`catalog_filter_for_student`)
- [x] `notifications/` — قناة داخل التطبيق + قناة بريد

## ✅ منجَز لاحقًا (2026-08-14 · 2026-08-15)

- [x] قوالب بريد معاملاتية ثنائية اللغة: استلام · تأكيد · شحن · تسليم · إلغاء · دفع
- [x] طابور المهام — **`ops/` لا Celery** (ADR-42 · القرار نقض ما كان مكتوبًا هنا)
- [x] كل واجهات متجر الطلاب (٢٩ شاشة)
- [x] `seo/` — `robots.txt` و`sitemap.xml` على **الجذر** لا تحت `/api/v1/`

## ⬜ متبقٍّ

- [ ] تجميع الترجمات (`compilemessages`) — **مؤجّل بقرارك إلى نهاية الخطة**
- [ ] فحص كل شاشة على ٣٦٠px و٧٦٨px و١٢٨٠px و١٩٢٠px — **فحص يدوي في متصفح**
- [ ] توليد مسبق لصفحات المنتجات (أثر ADR-37: SPA بلا SSR)

**بوابة الخروج:** اختبارات الترجمة تنجح · SEO مُتحقَّق منه على صفحات المنتجات · كل شاشة مُختبَرة على ٣٦٠px و٧٦٨px و١٢٨٠px و١٩٢٠px.

---

# ✅ المرحلة 7 — نقطة البيع POS (مكتملة 2026-08-15)

- [x] `pos/` — نطاق مستقل يستهلك `orders` · `payments` · `inventory` · `pricing` · `catalog`
- [x] `Register` مربوط بـ `StockLocation` (`PROTECT`) + بذرة ثلاثة أجهزة
- [x] **`POSSession`** — قيد فريد يمنع ورديتين مفتوحتين على جهاز · `expected_cash` **لقطة** لا تُعاد
- [x] **`CashMovement`** — حركات درج **إضافة فقط**، `save()` ترفع على التعديل
- [x] `checkout` ينشئ `Order` بـ `channel=POS` ← **لا نموذج طلب موازٍ**
- [x] بحث بالباركود (مطابقة تامة) و SKU والاسم — `/pos/products/`
- [x] **تسعير حيّ بلا أثر** — `/pos/quote/`، ونفس الدالة تُحصِّل (ADR-43)
- [x] **دفع مقسّم** — نقدي + بطاقة في فاتورة واحدة، وبوابة `pos-card` مبذورة
- [x] المرتجعات — `refund_pos_order` يعلّم `REFUNDED` ولا يحذف
- [x] إيصال + إعادة طباعة بـ `@media print` (بلا مكتبة PDF)
- [x] خصم المخزون فورًا من موقع الجهاز — بلا حجز
- [x] تسوية الوردية: عدّ → فرق → تفسير إلزامي فوق الحد
- [x] صلاحيات: `CanOperatePOS` (موظف/أدمن) · `CanRefund` (أدمن)
- [x] واجهة لمسية للتابلت — ثلاثة أعمدة، وحقل بحث يستقبل الماسح ويعيد التركيز
- [x] شاشة ورديات في لوحة الأدمن — عمود الفرق مُبرَز بالوزن واللون معًا

**بوابة الخروج: ✅ محقَّقة.** ٤٢ اختبارًا، وتسوية حقيقية مُتحقَّق منها
حيًّا: افتتاح ٢٠٠ + نقدي ١٠٣٫٩٠ (بلا بطاقة ٥٠) = متوقَّع ٣٠٣٫٩٠ ·
فرق ٠٫٠٠ · المخزون ٨٠ ← ٧٧.

**⚠️ ما يبقى معلّقًا:** قاعدة العمل ١١ (صلاحيات الكاشير) مطبَّقة
بافتراضٍ **متشدّد** لا محسومة — لا خصم (سقف صفر) ولا ارتجاع بلا
أدمن. توسيعها قرارك.

---

# ✅ المرحلة 8 — المالية: الإيرادات والمصروفات (مكتملة 2026-08-15)

- [x] `finance/` — نطاق مستقل **يستمع ولا يُستدعى**، فوق `pos` و`orders`
- [x] **`ExpenseCategory`** شجرية — ١٣ بندًا مبذورًا، والمرافق مفصّلة (كهرباء · مياه · إنترنت)
- [x] **`Expense`** — مبلغ · بند · تاريخ استحقاق · مورّد · مرفق فاتورة · طريقة دفع · مُدخِل · اعتماد
- [x] **`RevenueEntry`** — التقاط تلقائي، و**قيد فريد يمنع ازدواج الإيراد**
- [x] **`COGSEntry`** — من `Batch.unit_cost` عبر حركة البيع (FEFO) لا من متوسط
- [x] **`FiscalPeriod`** — إقفال شهري يمنع التعديل بأثر رجعي
- [x] تقرير **P&L** كامل + تفصيل «أين صُرف المال» + الإيراد بالقناة
- [x] **التدفق النقدي مشتقًّا لا مخزَّنًا** — الوارد من `payments` والصادر من المصروفات
- [x] **صلاحيات مالية منفصلة** — `view_revenueentry` · `add_expense` · `change_expense`
- [x] شاشتا الأدمن: قائمة الأرباح (معادلة متدرّجة) والمصروفات (إدخال · اعتماد · رفض)

**بوابة الخروج: ✅ محقَّقة.** ٣٤ اختبارًا، وتحقّق حيّ على بيانات البذرة:
صافي مبيعات ٣٨٢٣٫٠٠ − تكلفة ٢٩٣٦٫٩٠ = مجمل ٨٨٦٫١٠ (هامش ٢٣٫١٨٪)،
والمعادلة توازن في الطرفين.

## ⚠️ ثلاثة قرارات خالفت المخطَّط — وكلها مقصودة

| المخطَّط | ما نُفِّذ ولماذا |
|---|---|
| `CashFlow` موديلًا | **مشتق لا مخزَّن.** الوارد يعرفه `payments` والصادر تعرفه المصروفات؛ تخزينه ثالثًا ينشئ رقمًا يجب أن يوازي مصدرين، وأول انحراف لا يملك أحد حسمه |
| تصدير Excel / PDF | **مؤجَّل.** التقرير يُقرأ من الشاشة، والتصدير يحتاج قرارك عن الصيغة والجمهور (محاسب خارجي؟ مصلحة الضرائب؟) قبل اختيار المكتبة |
| «مصروف يُدخَل ويُعتمَد» | **الاعتماد صلاحية ثالثة منفصلة.** من يُدخل ويعتمد بنفسه يجعل الاعتماد توقيعًا على بياض |

## ⚠️ خطأ ترتيب أمسكه هذا العمل في نقطة البيع

نقطة البيع تخصم المخزون **قبل** إنشاء الطلب (لئلا يبقى طلب يتيم لو
نفد صنف)، فكانت حركات المخزون مربوطة بالوردية لا بالطلب. والمالية
تحسب التكلفة من حركات الطلب — فكانت **كل بيعة كاونتر تُقيَّد بتكلفة
صفر**، أي بربح يساوي ثمن البيع كاملًا.

خطأ في الاتجاه الأسوأ: التقرير يبدو ممتازًا. أُصلح بإعادة توجيه
الحركات بعد إنشاء الطلب، ثم إعادة حساب التكلفة عند
`pos_sale_completed`. يحرسه `TestPOSCostIsNotZero` — وتحقّقتُ من
سقوطه فعلًا بتعطيل الإصلاح.

---

# ✅ المرحلة 9 — B2B الصيدليات (مكتملة 2026-08-15)

> **المبدأ الحاكم:** الآجل هو كل الفرق بين B2C وB2B. عميل التجزئة
> يدفع ثم يستلم؛ والصيدلية تستلم ثم تدفع بعد ثلاثين يومًا — وهذا
> يقلب المخاطرة: البضاعة تخرج والمال لم يدخل.

- [x] `b2b/` — نطاق مستقل، شقيق `finance` فوق `orders`
- [x] **`BusinessProfile`** — نوع المنشأة · الترخيص وانتهاؤه · الحد · المهلة · من منح ومتى
- [x] **`LedgerEntry`** — دفتر حساب **إضافة فقط**، والرصيد **مشتق منه لا مخزَّن**
- [x] **`Invoice`** — لقطة مبالغ وقت الإصدار، وتأخّرها يُحسب لحظيًا
- [x] **بوابة ائتمان** بأربعة أسباب مرتّبة: موقوف · بلا ائتمان · ترخيص منتهٍ · متأخرات · تجاوز الحد
- [x] `select_for_update` يمنع تجاوز الحد بطلبين متزامنين
- [x] إتمام شراء آجل `/b2b/checkout/` — طلب + فاتورة + قيد في معاملة واحدة
- [x] **كشف حساب** برصيد افتتاحي وحركات وختامي + **تقادم** بخمس سلال
- [x] السداد يُسوّي **الأقدم أولًا**، والجزئي لا يغلق فاتورة
- [x] المرتجع **إشعار دائن** لا حذف للقيد الأصلي
- [x] إعادة طلب سريع — **بالأكثر تكرارًا لا الأحدث**
- [x] صلاحية `CanManageCredit` منفصلة — رفع الحد قرار بحجم القرض
- [x] شاشة العميل (ملخّص · فواتير · كشف) وشاشة الأدمن (منح · سداد · إيقاف)
- [x] أسعار الجملة **كانت جاهزة** — قائمة `wholesale` مربوطة بأنواع الحسابات التجارية

**بوابة الخروج: ✅ محقَّقة.** ٣٨ اختبارًا، منها اختبار تزامن حقيقي
بخيطين ومعاملتين: حدّ ٥٠٠٠ وطلبان بـ٣٠٠٠ ⟵ يمرّ **واحد فقط**.
وتحقّقتُ من سقوطه بتعطيل القفل: يمرّ الاثنان (`['ok','ok']`).

## ⚠️ ثلاثة فخاخ أمسكها البناء

| الفخّ | الأثر لو مرّ |
|---|---|
| `OVERDUE` مستبعدة من استعلامات «المفتوح» | تعليم الفاتورة متأخرةً **يخفيها** من بوابة المنع — فتسقط أقدم الديون بمجرد أن تصير أقدم |
| `available_credit` بأرضية بلا سقف | عميل سدّد أكثر مما عليه يرفع متاحه **فوق** ما مُنح له فيشتري بحدٍّ لم يوافق عليه أحد |
| حارس «إضافة فقط» يفحص `self.pk` | المفتاح UUID يُولَّد قبل الإدراج، فالحارس كان يرفض **كل إنشاء** — أي يمنع ما جاء ليحرسه |

## ⚠️ ما لم يُبنَ — وهو مذكور في المتطلبات

| البند | الحالة |
|---|---|
| نقاط الولاء للصيدليات | **المرحلة ١٢** — محرك الولاء كامل هناك، وبناء نصفه هنا يُنتج نظامين |
| طباعة الفاتورة PDF | مؤجَّل مع تصدير المالية — يحتاج قرارك عن الصيغة والجمهور |
| تنبيه استحقاق قبل موعده | يحتاج `ops/run_periodic` + قالب بريد. `refresh_overdue_flags` جاهزة للجدولة |

---

# ⬜ المراحل 10-15

| # | المرحلة | أبرز المهام |
|---|---|---|
| 10 | بوابة الموظفين | `employees/` · أدوار وظيفية · **`CustomerAssignment`** · إنشاء طلب لعميل |
| 11 | التارجت والعمولات | `targets/` شهرية · `commissions/` بمحرك قواعد · لقطات أداء · أثر المرتجعات |
| 12 | الولاء والإحالة | نقاط · فئات · مكافآت · أكواد إحالة · سفراء جامعات · مكافحة التلاعب |
| 13 | التقارير | `reporting/` — مبيعات · مخزون · أداء موظفين · سلوك عملاء |
| 14 | الموردون | `suppliers/` — أوامر شراء · استلام · كشف حساب |
| 15 | Marketplace | منتج واحد من موردين متعددين |

---

# بوابات الجودة

## بوابة عامة تسري على كل المراحل

- [ ] `lint-imports` ينجح — صفر استيراد صاعد · صفر دائرة
- [ ] الـ migrations تنجح على قاعدة نظيفة
- [ ] اختبارات الصلاحيات تنجح
- [ ] اختبارات سياسات الوصول تنجح
- [ ] بناء الفرونت إند ينجح
- [ ] صفر خطأ حرج
- [ ] صفر ارتداد في الميزات القائمة
- [ ] **صفر صفحة تخالف [قواعد بنية المعلومات](../frontend/02-INFORMATION-ARCHITECTURE.md)**
