# ═══════════════════════════════════════════════════════════
#  اعتماديات المشروع — مباشرة فقط
#
#  ⚠️  **لا تُولّد هذا الملف بـ `pip freeze`.**
#
#      الأمر يكتب كل ما في البيئة بما فيه التبعيات غير المباشرة
#      وبقايا حزم قديمة، فيعيد التلوّث الذي نُظّف في المرحلة 0.1
#      (PyQt5 · allauth · taggit …).
#
#      أضف الحزمة الجديدة هنا يدويًا في قسمها.
#
#  التثبيت:
#      python -m venv .venv
#      .venv\Scripts\activate      (ويندوز)
#      pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── الإطار ─────────────────────────────────────────────────
Django==5.2

# ── الإعدادات والبيئة ──────────────────────────────────────
django-environ==0.11.2

# ── واجهات REST ────────────────────────────────────────────
djangorestframework==3.16.0
djangorestframework-simplejwt==5.5.0
django-filter==25.1
drf-yasg==1.21.10
django-cors-headers==4.7.0

# ── بوابات الدفع الخارجية ──────────────────────────────────
# ⚠️  كل نداء بوابة له مهلة إلزامية — انظر payments/gateways/transport.py
#     النداء بلا مهلة يعلّق عامل ويب إلى الأبد حين تتوقف البوابة عن الرد.
requests==2.32.3

# ── المعرّفات ──────────────────────────────────────────────
uuid-utils==0.10.0

# ── قاعدة البيانات ─────────────────────────────────────────
# PostgreSQL هو المحرك المعتمد — SQLite لا ينفّذ select_for_update
psycopg2-binary==2.9.9

# ── الكاش ──────────────────────────────────────────────────
redis==5.0.1

# ── الوسائط ────────────────────────────────────────────────
Pillow==11.2.1

# ── الترجمة ────────────────────────────────────────────────
# بديل GNU gettext على ويندوز — ينتج .po و .mo قياسية
babel==2.18.0

# ── أدوات التطوير ──────────────────────────────────────────
django-debug-toolbar==4.4.6
Faker==20.1.0

# ── الجودة والاختبارات ─────────────────────────────────────
ruff==0.6.9
pytest==8.3.3
pytest-django==4.9.0
factory-boy==3.3.1
import-linter==2.1
pre-commit==4.0.1

# ═══════════════════════════════════════════════════════════
#  سجل الحذف
# ═══════════════════════════════════════════════════════════
#  django-allauth · dj-rest-auth
#      كانا مكسورين: AUTHENTICATION_BACKENDS يستبدلهما بالكامل.
#      استُبدلا بمصادقة مخصصة في المرحلة ١.
#
#  django-taggit          الوسوم تُعاد في catalog عند الحاجة
#  django-bootstrap5      طبقة القوالب حُذفت (قرار SPA)
#  PyQt5 وتوابعه          لا علاقة لها بمشروع ويب — تلوّث pip freeze
#  django-context-cache   وسيطها مُشار إليه في إعدادات ميتة
# ═══════════════════════════════════════════════════════════
