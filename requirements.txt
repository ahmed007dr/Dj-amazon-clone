# ═══════════════════════════════════════════════════════════
#  اعتماديات المشروع — مباشرة فقط
#  التبعيات غير المباشرة يحلّها pip
#
#  التثبيت:
#      python -m venv .venv
#      .venv\Scripts\activate      (ويندوز)
#      pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── الإطار ─────────────────────────────────────────────────
Django==5.2

# ── الإعدادات والبيئة ──────────────────────────────────────
django-environ==0.11.2

# ── واجهات REST ────────────────────────────────────────────
djangorestframework==3.16.0
djangorestframework-simplejwt==5.5.0
django-filter==25.1
drf-yasg==1.21.10
django-cors-headers==4.7.0

# ── المعرّفات ──────────────────────────────────────────────
uuid-utils==0.10.0

# ── قاعدة البيانات ─────────────────────────────────────────
psycopg2-binary==2.9.9

# ── الكاش والمهام الخلفية ─────────────────────────────────
redis==5.0.1

# ── الوسائط ────────────────────────────────────────────────
Pillow==11.2.1

# ── أدوات التطوير ──────────────────────────────────────────
django-debug-toolbar==4.4.6
Faker==20.1.0

# ── الجودة والاختبارات ─────────────────────────────────────
ruff==0.6.9
pytest==8.3.3
pytest-django==4.9.0
factory-boy==3.3.1
import-linter==2.1
pre-commit==4.0.1

# ═══════════════════════════════════════════════════════════
#  سجل التغييرات
# ═══════════════════════════════════════════════════════════
#  المرحلة ١
#    Django 4.2.16 → 5.2   (البيئة العامة عليه أصلًا)
#    DRF · simplejwt · django-filter · drf-yasg ← إصدارات متوافقة مع 5.2
#    + django-cors-headers   (الفرونت إند منفصل — ADR-03)
#    + uuid-utils            (UUIDv7 — ADR-26)
#    − django-allauth        استُبدل بمصادقة مخصصة · كان مكسورًا أصلًا
#                            (AUTHENTICATION_BACKENDS كان يستبدله بالكامل)
#    − dj-rest-auth          تبع allauth
#    − django-taggit         الوسوم تُعاد في catalog بالمرحلة ٣
#    − django-bootstrap5     طبقة القوالب حُذفت (قرار SPA)
#
#  المرحلة 0.1
#    − PyQt5 · django-context-cache · ~30 تبعية غير مباشرة
# ═══════════════════════════════════════════════════════════
