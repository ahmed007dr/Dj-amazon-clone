# ═══════════════════════════════════════════════════════════
#  Project dependencies — direct ones only
#
#  ⚠️  **Do not generate this file with `pip freeze`.**
#
#      That command writes everything in the environment, including transitive
#      dependencies and leftovers of old packages, reintroducing the pollution cleaned up in phase 0.1
#      (PyQt5 · allauth · taggit …).
#
#      Add a new package here by hand, in its own section.
#
#  Installation:
#      python -m venv .venv
#      .venv\Scripts\activate      (Windows)
#      pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── Framework ──────────────────────────────────────────────
Django==5.2

# ── Configuration and environment ──────────────────────────
django-environ==0.11.2

# ── REST interfaces ────────────────────────────────────────
djangorestframework==3.16.0
djangorestframework-simplejwt==5.5.0
django-filter==25.1
drf-yasg==1.21.10
django-cors-headers==4.7.0

# ── External payment gateways ──────────────────────────────
# ⚠️  Every gateway call has a mandatory timeout — see payments/gateways/transport.py
#     A call without one pins a web worker forever once the gateway stops responding.
requests==2.32.3

# ── Encryption ─────────────────────────────────────────────
# ⚠️  Gateway keys are stored encrypted — see core/encryption.py
#     They used to be plaintext in the database: one leaked backup was enough
#     to move real money.
cryptography==50.0.0

# ── Identifiers ────────────────────────────────────────────
uuid-utils==0.10.0

# ── Database ───────────────────────────────────────────────
# PostgreSQL is the supported engine — SQLite does not implement select_for_update
psycopg2-binary==2.9.9

# ── Cache ──────────────────────────────────────────────────
redis==5.0.1

# ── Media ──────────────────────────────────────────────────
Pillow==11.2.1

# ── Translation ────────────────────────────────────────────
# A GNU gettext substitute on Windows — produces standard .po and .mo files
babel==2.18.0

# ── Development tools ──────────────────────────────────────
django-debug-toolbar==4.4.6
Faker==20.1.0

# ── Quality and testing ────────────────────────────────────
ruff==0.6.9
pytest==8.3.3
pytest-django==4.9.0
factory-boy==3.3.1
import-linter==2.1
pre-commit==4.0.1

# ═══════════════════════════════════════════════════════════
#  Removal log
# ═══════════════════════════════════════════════════════════
#  django-allauth · dj-rest-auth
#      Both were broken: AUTHENTICATION_BACKENDS replaces them entirely.
#      Replaced by custom authentication in phase 1.
#
#  django-taggit          tags will be reintroduced in catalog when needed
#  django-bootstrap5      the template layer was removed (SPA decision)
#  PyQt5 and friends      unrelated to a web project — pip freeze pollution
#  django-context-cache   its middleware was referenced from dead settings
# ═══════════════════════════════════════════════════════════
