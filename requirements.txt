# ═══════════════════════════════════════════════════════════
#  اعتماديات المشروع — مباشرة فقط
#  التبعيات غير المباشرة يحلّها pip
#
#  التثبيت:
#      python -m venv .venv
#      .venv\Scripts\activate      (ويندوز)
#      pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── الإطار ─────────────────────────────────────────────────
Django==4.2.16

# ── الإعدادات والبيئة ──────────────────────────────────────
django-environ==0.11.2

# ── واجهات REST ────────────────────────────────────────────
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
django-filter==23.5
drf-yasg==1.21.7

# ── المصادقة ───────────────────────────────────────────────
django-allauth==0.60.1
dj-rest-auth==5.0.2

# ── قاعدة البيانات ─────────────────────────────────────────
psycopg2-binary==2.9.9

# ── الكاش والمهام الخلفية ─────────────────────────────────
redis==5.0.1

# ── الوسائط ────────────────────────────────────────────────
Pillow==10.1.0

# ── التصنيفات ──────────────────────────────────────────────
django-taggit==5.0.1

# ── القوالب (تُحذف في المرحلة 0.5 — قرار SPA) ─────────────
django-bootstrap5==23.4

# ── أدوات التطوير ──────────────────────────────────────────
django-debug-toolbar==4.2.0
Faker==20.1.0

# ═══════════════════════════════════════════════════════════
#  حُذف في المرحلة 0.1
# ═══════════════════════════════════════════════════════════
#  PyQt5 / PyQt5-Qt5 / PyQt5-sip  — لا علاقة لها بمشروع ويب
#                                   (تلوّث من pip freeze على بيئة عامة)
#  django-context-cache           — وسيطها مُشار إليه في الإعدادات
#                                   القديمة والحزمة غير مستخدمة فعليًا
#  ~30 حزمة تبعيات غير مباشرة     — يحلّها pip تلقائيًا
# ═══════════════════════════════════════════════════════════
